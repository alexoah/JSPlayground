/**
 *	from JavaScript Comparisons: Exercise 4 ( https://www.w3schools.com/js/exercise_js.asp?filename=exercise_js_comparisons4 )
 *
 *	question:
 *	Choose the correct conditional (ternary) operator to alert "Too young" if age is less than 18, otherwise alert "Old enough".
 *
 *	var age = n;
 *	var voteable = (age _ 18) _ "Too young" _ "Old enough";
 *	alert(voteable);
 *
 */
var age = n;
var voteable = (age < 18) ? "Too young" : "Old enough";
alert(voteable);